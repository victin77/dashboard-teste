{
  "name": "dashboard-comissoes",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && rm -rf ../public && mv dist ../public",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
